// This file can be removed since auth functionality is not needed
// Keeping basic structure in case needed later

export const isClient = typeof window !== 'undefined'

// Placeholder for future auth utilities
export const auth = {
  // Add auth functions when needed
}